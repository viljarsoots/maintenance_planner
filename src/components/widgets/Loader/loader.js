import React from 'react';
import ReactLoading from 'react-loading';
 
const Example = ({ type, color }) => (
    <ReactLoading type={'SpinningBubblese'} color={'red'} height={667} width={375} />
);
 
export default Example;